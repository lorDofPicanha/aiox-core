# =============================================================================
# Livro Caixa ENIAC — Environment Variables
# Copy to .env.local for dev. Production lives in Vercel.
# =============================================================================

# -----------------------------------------------------------------------------
# Supabase (Postgres + RLS por empresa via company_members)
# -----------------------------------------------------------------------------
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# Server-only — NUNCA expor no bundle do cliente.
SUPABASE_SERVICE_ROLE_KEY=

# -----------------------------------------------------------------------------
# Copiloto IA (OpenAI) — opcional. Sem a chave, o assistente mostra "não configurado".
# -----------------------------------------------------------------------------
OPENAI_API_KEY=
# Modelo (padrão gpt-4o-mini). Ex.: gpt-4o, gpt-4o-mini
OPENAI_MODEL=gpt-4o-mini

# -----------------------------------------------------------------------------
# App
# -----------------------------------------------------------------------------
NODE_ENV=development
NEXT_PUBLIC_APP_URL=http://localhost:3010
