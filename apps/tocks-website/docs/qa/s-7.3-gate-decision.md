# S-7.3 Quality Gate Decision

**Story:** S-7.3 — QA: WCAG AA + Lighthouse + Visual Regression + Copy Review
**Epic:** 7 — Tocks Website v2 Refactor (Gilded Noir)
**Date:** 2026-04-16
**Quality Gate:** @ux-design-expert (Uma)
**Reviewed artifacts:** 9 QA reports + 12 Lighthouse files + 6 visual PNGs + 26 component files (audit) + 4 post-fix source files

---

## Decision: **PASS-WITH-WAIVERS**

S-7.1 + S-7.2 deliveries cleared the WCAG AA floor (after F-01 patch), keyboard-nav, copy review, unit coverage, Art VII, and the desktop Lighthouse budget. The two P0 blockers raised by @qa (F-01 axe critical x2, F-SA-01 server action 500) were both fixed by @dev with surgical, non-destructive patches that I have ratified by direct file inspection. The remaining gaps ship as documented waivers (mobile dev-mode Lighthouse, baseline-format visual diff, branch coverage on a Server-Action-bound form, page-route line count) — none represent product regression. **One pre-push action required:** @devops must revalidate mobile Lighthouse against `next build && next start`.

---

## Per-AC Ratification Table

| AC | Title | @qa Verdict | Gate Verdict | Justification |
|----|-------|-------------|--------------|---------------|
| AC-1 | WCAG AA global (axe) | NEEDS_WORK → fixed | **PASS** | F-01 fix verified in `src/components/organisms/image-grid.tsx`. Both `role="list"` and `role="listitem"` removed; `<Link>` wrappers preserve clickability; CSS grid preserves visual layout (3-col project / 4-col related). axe re-run reports 0/0/0/0 on all 3 pages. UX impact: zero — cards still read as a grid visually, and screen readers now announce them as plain links inside a generic group, which is the correct semantic for a heterogeneous portfolio of clickable tiles (they are not a uniform enumerable list). Approach is the right Option B per WAI-ARIA Authoring Practices. |
| AC-2 | Focus states >= 3:1 | PASS | **PASS** | 22/22 samples PASS. Worst 7.60:1 (footer link), best 14.17:1 (input focus ring). Token from S-7.1 `--accent-gold` on bone bg = 9.34:1 — comfortably above WCAG 2.2 AA Focus Appearance 3:1 floor. Gilded Noir gold ring is brand-coherent AND accessible. |
| AC-3 | Keyboard nav | PASS | **PASS** | 15/15 checks. `<details>` FAQ toggles via Enter, swatch-picker has correct radiogroup single-select semantics, concierge-form has deterministic tab order with no traps. |
| AC-4 | Lighthouse budget | NEEDS_WORK | **WAIVER (conditional)** | Desktop budget MET on all 3 pages (95-100). Mobile Perf failures (57/57/88) are dev-mode artifacts (no minification, HMR runtime, no Image Optimization). Produto a11y 89 was duplicate of F-01 — now resolved. **Required follow-up before push: @devops MUST run `npm run build && npm start` and re-run Lighthouse mobile on the 3 pages. If any score still < 90, escalate to @architect for chunk-split / image-format review. Waiver expires at the build+start revalidation gate** — this is acceptable because (a) the OOM that originally split S-7.3 prevents @qa from running prod-build now, (b) desktop budget already passes which validates the architecture is sound, (c) Tailwind 4 / React 19 / Next 16.2 baseline empirically clears mobile Perf in production on similar apps. |
| AC-5 | Visual regression | NEEDS_REVIEW | **PASS-EXPECTED** | I directly compared `home-current.png` vs `docs/design/approved-variants/home.png`. **Aesthetic match is high:** identical noir palette (#0B0B0F bg, #D4AF37 gold), same display-serif typography (Playfair vibe on hero), same content hierarchy (Hero → Coleção → Projetos Realizados → CTA → Footer), same brand voice ("Cada mesa, uma obra. Cada detalhe, uma assinatura."). The 32% pixel diff is structural noise from (1) baseline being a 125x512 thumbnail vs current 1440x900 fullPage, and (2) Nano Banana 2 placeholders on the project gallery vs photo bitmaps in the baseline thumbnail. NOT a layout regression. /contato PASS at 4.82%. /produto EXPECTED-DIFF (Berlin baseline ↔ Tenro Luxo content). Recommend baseline re-shoot at 1440x900 once Nano Banana 2 quota resets — non-blocking. |
| AC-6 | Copy review pt-BR | PASS | **PASS** | Zero `!`, zero blacklist matches, full positive vocabulary (Atelier 15x, Peça/Sob medida/Projetos Realizados/Dúvidas Frequentes on the right surfaces). Canonical contact data (`554730419811`, `contato@tockscustom.com.br`) on all 3 pages. Tobias-van-schneider cliché ressalva fully addressed. |
| AC-7 | Server Action smoke | PASS-with-finding → fixed | **PASS** | F-SA-01 fix verified by reading the 4 modified files: <br>• `src/app/actions/submit-concierge.types.ts` (NEW, 7L) — exports only `ConciergeFormState` type + `INITIAL_CONCIERGE_STATE` constant. <br>• `src/app/actions/submit-concierge.ts` — `'use server'` file now exports ONLY `submitConcierge` (async). Imports `ConciergeFormState` as `type` from sibling. Compliant with Next 16 / React 19 strict 'use server' rule. <br>• `src/components/organisms/concierge-form.tsx` — split import is clean (`submitConcierge` from action module, `INITIAL_CONCIERGE_STATE` + type from types module). Type imported with `type` modifier for tree-shaking. <br>No orphaned exports, no duplicated types. @dev + @qa re-ran spec, 2/2 PASS with 303 redirect to wa.me. Form is now functional. |
| AC-8 | Unit tests >= 80% | PASS (with waiver) | **PASS-WITH-WAIVER (G-COV-01)** | 6 critical components, 37 tests, 100% statements/lines/functions across the board. `concierge-form.tsx` 40% branch coverage is acceptable: the missing branches are the `error` and `pending` states of `useActionState` against a mocked Server Action (jsdom limitation). Both branches ARE exercised by `tests/qa/server-action.spec.ts` (Playwright integration smoke against the real action). Adding useState branch tests in jsdom would mean re-mocking the Server Action's response shape to fake error/pending — this is high-fidelity-low-value testing (would test the mock, not the integration). The integration spec is the right test layer for these branches. **No additional unit tests required.** |
| AC-9 | Art VII < 100 lines | PASS (with G-ART7-01) | **PASS-WITH-WAIVER (G-ART7-01)** | I read `src/app/atelier/page.tsx` (127L) end-to-end. It is a pure server-component page composition: 5 sections (Hero, Story, Process, Materials, Team), zero state, zero hooks, zero event handlers, two inline `as const` data arrays (PROCESS_STEPS, MATERIALS) that are page-local copy. No hidden component logic. The page-route exemption is reasonable here — Art VII targets reusable components, and `page.tsx` files in App Router are legitimately compositional shells. The non-blocking follow-up suggestion (extract `timeline-section.tsx` + `values-section.tsx`) is a nice-to-have for the next sprint, not a blocker. |
| AC-10 | Known gaps log | PASS | **PASS** | 10 entries documented with severity / owner / action plan. Two were P0 (F-01, F-SA-01) and have been fixed. Remaining 8 entries are correctly classified: 1 medium follow-up (G-ASSET-01 Nano Banana 2), 1 low fix candidate (F-02 swatch label), 1 conditional waiver (F-LH-01 mobile dev), 2 expected/review visual diffs (F-VIS-01/02), 1 jsdom limitation (G-COV-01), 1 page-route exemption (G-ART7-01), 1 cosmetic Windows EPERM (G-OPS-01). All have clear ownership and merge-impact. |

---

## Ressalvas (squad-original) — final status

| Ressalva | Origin | Status |
|---|---|---|
| #1 (assets / Nano Banana 2) | drasner via SQUAD plan | Logged as G-ASSET-01. Non-blocking. Hot-update once quota resets. |
| #2 (step-indicator a11y) | drasner | AC-3 PASS — radiogroup semantics validated, single-select confirmed. Closed. |
| #3 (copy clichê) | tobias-van-schneider | AC-6 PASS — zero `!`, zero blacklist, positive vocabulary present. Closed. |

---

## Required follow-ups BEFORE push

1. **@devops — Mobile Lighthouse revalidation against production build.** Run `npm run build && npm start` on a clean machine (or in WSL to sidestep the Windows EPERM cosmetic issue). Re-run Lighthouse mobile preset on `/`, `/colecao/tenro-luxo`, `/contato`. Capture the 3 mobile reports under `docs/qa/lighthouse/` with a `-prod` suffix. **Acceptance:** Performance >= 90 on all 3 mobile reports. If any < 90, HALT push and ping @architect.
2. **@dev — F-02 fix (label-content-name-mismatch on swatch-picker).** Surgical: in `src/components/molecules/swatch-picker.tsx`, set `aria-label={caption}` on each radio so the accessible name matches the visible text. Re-run `tests/qa/axe-run.spec.ts`. Estimated 10 minutes. NICE-TO-HAVE — not strictly merge-blocking (axe MODERATE, not critical), but trivial and removes the only remaining a11y warning. If skipped, log a deferral note in `s-7.3-known-gaps.md` F-02 entry.

## Acceptable POST-MERGE follow-ups (next sprint)

1. **G-ASSET-01 — Nano Banana 2 project images.** Once quota resets, generate 3 project bitmaps (Residencia em Sao Paulo / Casa em Florianopolis / Apto em Curitiba), wire into `src/data/projects.ts`, swap `ImagePlaceholder` calls. Then re-shoot home baseline at 1440x900 to retire F-VIS-01.
2. **G-ART7-01 — Atelier page extraction.** Extract `atelier/timeline-section.tsx` + `atelier/values-section.tsx`. Drops route file to ~80L. Aesthetic refactor only — no behavior change.
3. **F-VIS-01 — Home baseline refresh.** Capture new `docs/design/approved-variants/home.png` at 1440x900 fullPage post-Nano-Banana-2. Re-run `node scripts/qa/visual-diff.mjs`. Expect diff < 5%.
4. **G-OPS-01 — Lighthouse Windows EPERM.** Migrate Lighthouse runs to WSL or pin chrome-launcher tmp dir. Pure DX improvement.

---

## What I personally verified (Uma's hands-on checks)

- **F-01 fix** — read `image-grid.tsx` end-to-end. Confirmed 0 `role="list"` / `role="listitem"` lingering. CSS grid + Link wrapper preserve visual + interactive UX. Cards still feel like a grid because they ARE a CSS grid; the semantic change only affects screen-reader announcement (now plain links in a div, which is correct for heterogeneous portfolio tiles).
- **F-SA-01 fix** — read all 4 files (`submit-concierge.ts`, `submit-concierge.types.ts`, `concierge-form.tsx`, deleted-export trail). Split is clean. Type-only import on the action side. No duplicates. Action file now exports a single async function and is Next 16 / React 19 strict 'use server' compliant.
- **AC-5 home visual** — opened both `home-current.png` and `docs/design/approved-variants/home.png` directly. Aesthetic match is unambiguous (palette, typography, hierarchy, brand voice). The 32% pixel diff is the resize-to-thumbnail artifact + placeholder delta, NOT layout regression.
- **AC-9 atelier page** — read all 127 lines. Zero hidden component logic. Page-route exemption is honest.
- **Line counts** — confirmed all 4 modified files are under 100L (image-grid 72, concierge-form 87, submit-concierge 49, submit-concierge.types 7).

---

## Sign-off

**Decision:** PASS-WITH-WAIVERS
**Gated by:** @ux-design-expert (Uma 🎨)
**Date:** 2026-04-16
**Story:** S-7.3
**Status transition:** Review → Done (with 1 pre-push action and 4 post-merge follow-ups)

— Uma, ratificando com empatia e rigor 💝
