{
  "name": "@synkra/contador-nfse-client",
  "version": "0.1.0",
  "private": true,
  "description": "Typed, safety-gated client for the NFS-e Nacional SEFIN APIs.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "prepare": "npm run build",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "npm run build && node scripts/run-contract-tests.mjs"
  },
  "engines": {
    "node": ">=18"
  }
}
