#!/usr/bin/env sh

# AIOS Post-Commit Hook (Story ACT-3)
# Clears project status cache so next agent activation sees fresh data.
# Uses Node.js for cross-platform compatibility.

node .aios-core/infrastructure/scripts/git-hooks/post-commit.js 2>/dev/null || true
