# R0.5 — RESUME (retomar 10/Jun) — Closed Beta Anipis Launch

**Estado em 09/Jun fim do dia.** Beta de saúde mental, ~20 convidados. Quase no ar.

## 🟢 NO AR / FEITO

| Item | Detalhe |
|---|---|
| **API (Railway)** | `https://anipis-production.up.railway.app` — `/health` 200 ✅ |
| **Kill-switch** | testado ao vivo, `durable:true` (Upstash conectado) ✅ |
| **Redis (Upstash)** | `just-pug-96035.upstash.io` — conectado; rate-limit + crisis dedup duráveis |
| **Telegram crise** | `@anipis_bot`, chat `8521098317` (Breno) — mensagem-teste [RED] entregue ao vivo ✅ |
| **DB (Supabase)** | `qeaotfetegqcbueumxdi` — migration 011 já aplicada (3 profiles) |
| **Código** | commitado+pushado em `github.com/lorDofPicanha/anipis` main @ `9b1ccd1`. 922 testes verdes |
| **ZDR + DPA** | ambos adiados pós-CNPJ (conta OpenAI não-Business). Base legal = consentimento Art.33 VIII + Termos API + pseudonimização. Docs honestos (DPIA/decisão/política). `OPENAI_ZDR_WAIVED_ACK=true` em prod |
| **Web build (Vercel)** | compila 100% (29 rotas). Fix de deploy: Root Directory=`apps/web` + overrides **Build Command=`next build`** + **Output Directory=`.next`** |

## 🔴 PENDENTE — RETOMAR AQUI

### 1. ✅ RESOLVIDO 10/Jun: Web dá 500 em runtime (`MIDDLEWARE_INVOCATION_FAILED`)
> **Fix aplicado:** projeto `anipis-api` (conta `brenodecerqueira-4418` — era uma conta só; "different team" era bug do CLI v50, atualizado p/ v54) tinha **zero env vars**. As 5 foram setadas via `node .../vercel/dist/vc.js env add NAME production --force --yes --no-sensitive --value "x"` (⚠️ pipe PowerShell contamina com BOM+CRLF; stdin é ignorado em agent-mode — só `--value` funciona) + `vercel redeploy`. Resultado: `/login` 200, CSP com as 3 URLs limpas. Middleware também blindado contra env faltante (commit `0df135f`). Detalhe histórico abaixo:

- URL Web (Vercel): `https://anipis-api.vercel.app` — todas as rotas 500.
- **Causa:** `src/middleware.ts` faz `createServerClient(process.env.NEXT_PUBLIC_SUPABASE_URL!, NEXT_PUBLIC_SUPABASE_ANON_KEY!)` — se undefined, crasha em toda rota.
- **Fix:** Vercel → Settings → Environment Variables → confirmar as 5 setadas pra **Production** → **REDEPLOY** (NEXT_PUBLIC_* só embute no build):
  ```
  NEXT_PUBLIC_SUPABASE_URL=https://qeaotfetegqcbueumxdi.supabase.co
  NEXT_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_RkGNvrhDLBsG7AK0y-2GpA_TDZRkoaP
  NEXT_PUBLIC_API_URL=https://anipis-production.up.railway.app
  NEXT_PUBLIC_API_WS_URL=wss://anipis-production.up.railway.app
  INTERNAL_DASHBOARD_KEY=f46b8a5ffe6dc696a72a2edc05267bb9f43173316861756b7aa23b3cc974d325
  ```
- Provável: env vars adicionadas DEPOIS do build, ou não marcadas Production. Redeploy resolve.
- **Claude re-testa** as rotas do prod após redeploy.

### 2. Ver o FRONTEND (pedido do founder p/ amanhã)
- Depois do 500 resolvido: abrir `https://anipis-api.vercel.app` e revisar visual (home, /login, /onboarding, /chat, /privacidade). Design canônico = **verde floresta `#2f5235` sobre creme** (ver [[reference_anipis_design_green]]).

### 3. CORS — depois do Web no ar
- Railway → API → Variables → adicionar `CORS_ORIGIN=https://anipis-api.vercel.app` (senão browser bloqueia chamadas Web→API) → redeploy API.

### 4. Invite codes (20)
- Endpoint: `POST https://anipis-production.up.railway.app/internal/beta-invites` (header `X-Internal-Key: cc30329697b593c4f326d798656bd12887725327f63a570c730c01dabd60cee7`), body `{emails:[...20...], invited_by:"breno"}`. Retorna invite_url por e-mail.
- ⚠️ Sistema é **email-bound** — Claude precisa dos **20 e-mails reais** dos convidados. URLs apontam `<web>/invite/<code>`.

### 5. Smoke ao vivo dos 5 críticos (após Web ok)
- onboarding+consent / chat / **crise** (frase real → CVV/SAMU + alerta Telegram) / export / **delete (Art.18)**.

### 6. Follow-ups não-bloqueantes
- Kill-switch in-memory fallback (defesa-em-profundidade p/ Redis cair) — Upstash já cobre o caso normal.
- Sentry DSN (monitoria) — adiado.
- Sempre que mexer em `packages/shared`: rodar `node scripts/sync-api-shared.mjs` + commitar `_shared`.

## URLs/credenciais (todas no `.env.local` gitignored de `apps/api`)
API Railway · Web Vercel `anipis-api.vercel.app` · Supabase `qeaotfetegqcbueumxdi` · Upstash `just-pug-96035` · repo `lorDofPicanha/anipis`.

**Trigger retomada:** `retoma launch anipis` / `arruma o 500 do vercel` / `quero ver o frontend anipis`.
