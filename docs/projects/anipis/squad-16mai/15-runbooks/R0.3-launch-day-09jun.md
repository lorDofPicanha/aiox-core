# R0.3 — Launch Day Runbook (Closed Beta Anipis) — 09/Jun/2026

**Objetivo:** beta no ar hoje. Sequenciado por dependência. 🤖 = Orion/Claude faz · 👤 = founder faz.

---

## FASE 0 — Prep (🤖 ~15min)
- [ ] Checar migration 011 (read-only) contra Supabase
- [ ] Boot local da API com env novo (pega erro de config antes do Railway)
- [ ] Gerar bloco de env pronto pro Railway

### ✅ Resultados Fase 0 (09/Jun)
- **Migration 011:** JÁ APLICADA no banco do beta (`qeaotfetegqcbueumxdi`) — coluna + índice existem, 3 profiles. Risca da lista.
- **Boot local:** ✅ API sobe, `/health` 200 com o env atual (Telegram + gate ZDR).
- **Kill-switch:** bug pego ao vivo (sem Redis o OFF dava 503 e não desligava) → **corrigido** (OFF sempre engata, 200+durable:false). 922 testes verdes.
- **Port fix:** `server.ts` agora lê `$PORT` do Railway primeiro (evita "sobe mas Railway não alcança").

### 📋 Bloco de ENV — Railway (2 serviços)

**Serviço API** — `apps/serenity-ai/apps/api`
| Var | Origem | Crítico? |
|---|---|---|
| `NODE_ENV=production` | novo | 🔴 |
| `DATABASE_URL` | copiar do `.env.local` | 🔴 |
| `SUPABASE_URL` / `SUPABASE_ANON_KEY` / `SUPABASE_SERVICE_ROLE_KEY` | copiar | 🔴 |
| `OPENAI_API_KEY` | copiar | 🔴 |
| `OPENAI_ZDR_WAIVED_ACK=true` | novo (decisão no-ZDR) | 🔴 **sem isso não sobe** |
| `INTERNAL_API_KEY` | copiar (ou gerar novo p/ prod) | 🔴 kill-switch+analytics |
| `TELEGRAM_BOT_TOKEN` / `TELEGRAM_CRISIS_CHAT_ID` | copiar | 🔴 alerta de crise |
| `CORS_ORIGIN` | = URL do Web em prod | 🔴 |
| `UPSTASH_REDIS_URL` / `UPSTASH_REDIS_TOKEN` | criar Upstash (free) | 🟠 kill-switch durável |
| `SENTRY_DSN_API` | criar Sentry | 🟡 monitoria |
| `TELEGRAM_WEBHOOK_SECRET` / `TELEGRAM_LINK_BASE_URL` | copiar | ⚪ inertes hoje |
| (porta) | Railway injeta `$PORT` (já tratado no código) | — |

**Serviço Web** — `apps/serenity-ai/apps/web` (Next.js)
| Var | Origem | Crítico? |
|---|---|---|
| `NEXT_PUBLIC_API_URL` | = URL da API em prod | 🔴 |
| `NEXT_PUBLIC_SUPABASE_URL` / `NEXT_PUBLIC_SUPABASE_ANON_KEY` | copiar | 🔴 |
| `INTERNAL_API_KEY` | = mesma da API | 🟠 dashboard de retenção |
| `NEXT_PUBLIC_SENTRY_DSN` | criar Sentry | 🟡 |

## FASE 1 — Credenciais (👤 ~30min, paralelo)
- [ ] Aceitar **DPA padrão da OpenAI** no painel da org
- [ ] **Upstash Redis**: criar conta → `UPSTASH_REDIS_URL` + `UPSTASH_REDIS_TOKEN` (ou lançar com fallback in-memory — kill-switch não sobrevive restart)
- [ ] **Sentry**: criar projeto → `SENTRY_DSN_API` + `NEXT_PUBLIC_SENTRY_DSN` (ou adiar)
- [ ] Decidir host: URL Railway (recomendado) vs domínio

## FASE 2 — Deploy (👤 dirige + 🤖 assiste, ~30-45min)
- [ ] Railway: criar serviço, conectar repo `AIOS` (path `apps/serenity-ai`)
- [ ] Colar bloco de env (Fase 0). **Obrigatórios:** `DATABASE_URL`, `OPENAI_API_KEY`, chaves Supabase, `OPENAI_ZDR_WAIVED_ACK=true`, `TELEGRAM_BOT_TOKEN`, `TELEGRAM_CRISIS_CHAT_ID`, `INTERNAL_API_KEY`, `NODE_ENV=production`
- [ ] Deploy → confirmar boot (vai logar `[WARN] ... SEM OpenAI ZDR` = esperado) + `/health` verde
- [ ] Migration 011 em prod (se não aplicada na Fase 0 no mesmo banco)

## FASE 3 — Travar e validar (👤+🤖 ~30min)
- [ ] Travar signup nos ~20 convidados (whitelist)
- [ ] Smoke ao vivo dos 5 críticos: onboarding+consent / chat / **crise** / export / **delete**
- [ ] Alerta de crise → chega no Telegram do plantão clínico
- [ ] Kill-switch on/off (`POST /internal/kill-switch`)
- [ ] Sentry: erro sintético → zero PII (se Sentry ativado)

## FASE 4 — Lançar (👤 ~15min)
- [ ] E-mail/convite (template R4) com links /privacidade + /termos
- [ ] Disparar convites
- [ ] Monitorar 60min pós-envio (Telegram + logs)

---

**Pergunta de aprendizado do beta (já definida):** "tem retenção?" — volta espontânea D+7 ≥50% + sessões com profundidade. Leitura GO/NO-GO no R0.2.
