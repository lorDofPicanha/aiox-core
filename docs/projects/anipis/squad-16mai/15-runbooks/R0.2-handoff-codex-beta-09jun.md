# R0.2 — Handoff Codex: Fechamento do Closed Beta Anipis (não-código já decidido)

**Data:** 09/Jun/2026
**De:** Orion (aios-master / Claude) — raciocínio + conclave
**Para:** Codex (executor)
**Regra de ouro:** nada vira DONE sem evidência verificável (URL prod / query DB / teste PASS / screenshot).

---

## ⚡ EXECUTADO NESTA SESSÃO (09/Jun — Claude/Orion + agentes AIOS reais)

Em vez de handoff, o trabalho de código foi feito direto. Estado atual:

| # | Item-piso | Status | Evidência |
|---|---|---|---|
| 1 | Controlador PF nas páginas legais | ✅ FEITO | `legal-data.ts` com Breno de Cerqueira Pinheiro / CPF 088.203.901-67 / endereço **não público** (`controllerContactNote`). **DraftBanner → aviso de Beta vigente** (aios-dev) nas 2 páginas; typecheck PASS. Validado pelo clone patricia-peck. |
| 2 | Consentimento Art.11 + Art.33 | ✅ PASS | aios-qa: 44/44 testes |
| 3 | Crise RED-bypass + Telegram + classificador linguagem real | ✅ PASS | aios-qa: 349/349 testes |
| 4 | Disclaimer "não é terapeuta" 1º passo onboarding | ✅ PASS | aios-qa confirmado |
| 5 | Art.18 hard-delete + crisis-deletion-guard | ✅ PASS | aios-qa: 38/38 testes |
| 6 | Retenção d1/d7/d30 (queries SQL reais) | ✅ PASS (s/ teste auto) | aios-qa |
| 7 | Copy limpa (zero promessa terapia/cura) | ✅ PASS | aios-qa |
| 8 | **Kill-switch GLOBAL** (era CONCERNS) | ✅ FEITO | aios-dev: flag Redis flip-instantâneo, crisis path sobrevive, endpoint interno; **913/913 testes** (20 novos), typecheck limpo |

**Suíte API: 913/913 verde. Zero FAIL. Código do beta = GO.**

### ⛔ Só falta FOUNDER-GATED (Claude não consegue — credencial/prod):
1. ~~Telegram~~ ✅ **PROVADO 09/Jun** — token+chat id no `.env.local`, mensagem-teste [ANIPIS RED] entregue ao founder (Breno, chat 8521098317, msg_id=3). Vars: `TELEGRAM_BOT_TOKEN`, `TELEGRAM_CRISIS_CHAT_ID`, `TELEGRAM_WEBHOOK_SECRET`, `TELEGRAM_LINK_BASE_URL` (os 2 últimos inertes no código atual). **FALTA: replicar os 4 vars no ambiente de PROD (Railway).** Plantão clínico: founder recebe → aciona o clínico (definido).
2. ~~ZDR + DPA~~ **DECIDIDO 09/Jun: beta roda SEM ZDR e SEM DPA formal** (conta OpenAI não-Business; DPA self-serve exige Business → adiado pós-CNPJ). Base legal da transferência = **consentimento Art. 33 VIII** (suficiente sozinho) + Termos de API OpenAI + pseudonimização. Caminho honesto: gate `OPENAI_ZDR_WAIVED_ACK` (não falsifica nada); DPIA + decisão + política + página atualizados pra refletir a base real; registro em `12-compliance/DECISION-no-ZDR-closed-beta-09jun.md`. **FALTA founder:** só setar `OPENAI_ZDR_WAIVED_ACK=true` em prod. DPA + ZDR no roadmap pós-CNPJ.
3. `INTERNAL_API_KEY` em prod (kill-switch endpoint + dashboard de analytics).
4. Deploy prod + healthcheck PASS.
5. Migration `011_ai_features_enabled.sql` aplicada em prod (query confirma coluna).
6. Travar signup nos ~20 convidados (whitelist).
7. Sentry: disparar erro sintético com CPF fake → confirmar zero PII.
8. Smoke ao vivo dos 5 críticos: onboarding+consent / chat / **crise** / export / **delete**.

### Operação do kill-switch (guardar):
`POST /internal/kill-switch` com header `X-Internal-Key` e body `{"enabled": false}` desliga a IA de todos na hora (crise continua ativa). Fallback: `SET system:ai_globally_enabled 0` no Redis.

---

## Contexto travado (NÃO reabrir — já decidido pelo founder + conclave)

- **Produto:** Anipis — companheiro de IA de saúde mental. Dado sensível (Art. 11 LGPD) + transferência internacional pra OpenAI (Art. 33).
- **Escopo:** Closed beta, **~20 convidados que o founder contata diretamente**. Não é launch público.
- **Controlador:** **Pessoa Física** (o founder). Sem CNPJ. Sem MEI.
- **DPO interim:** Breno Cerqueira (já no código).
- **Plantão clínico:** **resolvido** — founder já tem profissional pra eventos RED. (Não é o founder o primeiro-respondente.)
- **Hipótese de aprendizado do beta:** **"tem retenção?"** — e especificamente **retorno NÃO-incentivado + profundidade da conversa**, não contagem de login (os 20 são conhecidos → login vem inflado pela relação).
- **Benchmark de realidade:** app de saúde mental retém ~3-4% em 30d (cold). Com convidados conhecidos será maior; o sinal honesto é volta espontânea + sessões com profundidade.

### FORA DE ESCOPO (não fazer agora — adiado pós-conclave)
DPO formal + registro ANPD · OAB carimbar SCC v2/DPIA (drafts bastam) · sistema de invite automatizado (whitelist manual basta) · Upstash região cutover · Langfuse self-host BR · domínio custom · CI hardening (gitleaks/npm audit) · smoke 100% · classificação ANVISA/CFP · **Miso One (voz) — é aposta de v2, NÃO entra no beta** (adiciona superfície sensível nova; não mede retenção).

---

## O achado que reduz o trabalho

Auditoria local (09/Jun) mostra que **quase tudo já está no código**. O trabalho é **VERIFICAR + FINALIZAR + plugar dados PF**, não construir. Cada item abaixo tem caminho real de arquivo.

---

## PISO DO BETA — 6 itens (estes NÃO caem, nem com 5 pessoas)

### 1. Controlador PF nas páginas legais  ⟵ destrava tudo, é trivial
- **Arquivo:** `apps/serenity-ai/apps/web/src/data/legal-data.ts` (linhas 12-15).
- **Dados PF reais (controlador Pessoa Física):**
  - Nome completo: **Breno de Cerqueira Pinheiro**
  - CPF: **088.203.901-67**
  - Endereço (arquivo): Rua Arno Lüders, 385 — Itoupava Norte, Blumenau/SC
- **Ação:** substituir os placeholders:
  - `companyPlaceholder: '[Razao Social da Empresa]'` → `Breno de Cerqueira Pinheiro` + rotular como **"Controlador (Pessoa Física)"**.
  - `cnpjPlaceholder: '[XX.XXX.XXX/XXXX-XX]'` → `088.203.901-67` e **renomear o rótulo de "CNPJ" para "CPF"** (PF não tem CNPJ).
  - `addressPlaceholder: '[Endereco Completo]'` → ✅ **DECIDIDO (09/Jun): NÃO publicar endereço.** Remover o campo de endereço da renderização pública das páginas legais. Canal de contato do controlador = **e-mail** (`suporte@anipis.com.br` / `dpo@anipis.com.br`). Endereço fica apenas em arquivo interno (disponível mediante solicitação à ANPD/titular). Justificativa: LGPD exige controlador identificável + contatável (nome + CPF + e-mail cumprem), não endereço físico público; evita exposição de segurança pessoal em app de saúde mental.
  - `dpoNamePlaceholder` → já OK (Breno Cerqueira interim).
- **Verificar render:** `apps/web/src/app/(legal)/privacidade/page.tsx` e `.../termos/page.tsx` — abrir e confirmar **zero `[...]` placeholder visível**, seção "2. Controlador e Encarregado" preenchida.
- **Aceite:** página `/privacidade` e `/termos` abrem em prod sem nenhum colchete-placeholder; controlador identificado (nome + CPF + canal de contato).

### 2. Consentimento Art. 11 (saúde sensível) + Art. 33 (transferência internacional) — DOIS consentimentos distintos
- **Arquivos:**
  - UI: `apps/web/src/components/features/ConsentFlow.tsx`, `apps/web/src/components/onboarding/InternationalTransferConsent.tsx`, `apps/web/src/data/consent-flow-texts.ts`
  - API: `apps/api/src/routes/granular-consents.ts`, `apps/api/src/services/consent-service.ts`
  - Gate: `apps/api/src/middleware/ai-consent-gate.ts`
- **Verificar:** existem **dois consentimentos separados e específicos** — (a) tratamento de dado sensível de saúde; (b) transferência internacional pra OpenAI no exterior. Um "aceito os termos" genérico NÃO cobre. O `ai-consent-gate` deve **bloquear o chat de IA** se qualquer um dos dois faltar.
- **Aceite:** usuário sem os dois consentimentos registrados **não alcança o chat**; consentimento gravado com timestamp + versão.

### 3. Roteamento de crise — testar com linguagem REAL
- **Arquivos:** `apps/api/src/routes/crisis.ts`, `apps/web/src/components/features/CrisisAlert.tsx`, contatos em `legal-data.ts` (`EMERGENCY_CONTACTS`: CVV 188, SAMU 192).
- **Verificar com frases reais** (não só a palavra "suicídio"): `"não aguento mais"`, `"queria sumir"`, `"to pensando em me machucar"`, `"não vejo sentido em continuar"`.
- **Notificação do evento RED (protocolo definido):**
  - Canal: **Telegram do founder** (Breno) — alerta em tempo real quando um evento RED dispara.
  - **Respondente clínico:** o plantão clínico que o founder já tem (nome a preencher) é quem efetivamente acompanha o caso. Founder recebe o alerta e **aciona o clínico** — founder NÃO é o primeiro-respondente (evita relação-dupla com convidados conhecidos).
  - Implementação: bot Telegram (token via env, ex. `TELEGRAM_BOT_TOKEN` + `TELEGRAM_CRISIS_CHAT_ID`). Codex provisiona; founder fornece o chat id dele.
- **Aceite:** cada frase dispara detecção → exibe CVV 188 + SAMU 192; dedup/cap por usuário funciona (não floda); **alerta chega no Telegram do founder** com timestamp + id do usuário; evento logado.

### 4. Disclaimer "não é terapeuta / não é emergência"
- **Arquivos:** `apps/web/src/components/features/BetaGuideDisclaimer.tsx`, `apps/web/src/app/(app)/beta-guide/page.tsx`.
- **Verificar:** disclaimer **visível antes do primeiro chat** (onboarding), claro que não substitui terapia e não é canal de emergência.
- **Aceite:** usuário novo vê o disclaimer no primeiro uso.

### 5. Exclusão Art. 18 (hard delete) funcionando
- **Arquivos:** `apps/api/src/services/account-deletion-service.ts`, `apps/api/src/routes/account.ts`, guard: `apps/api/src/services/crisis-deletion-guard.ts`.
- **Testes existentes:** `__tests__/ci/art18-completeness-gate.test.ts`, `account-deletion-lgpd-art18.test.ts`, `account-deletion-order.test.ts`.
- **Ação:** rodar os testes + **fazer um delete real de usuário fake** e confirmar no DB que os dados sumiram.
- **Aceite:** testes Art.18 PASS + delete manual verificado por query em prod (linha some das tabelas relevantes).

### 6. Instrumentação de retenção LIGADA (a razão de ser do beta)
- **Arquivos:** API `/internal/analytics` (consumida por `apps/web/src/app/internal/dashboard/page.tsx`); tipos em `apps/web/src/types/analytics.ts` (`AnalyticsResponse`: retention d1/d7/d30, DAU/WAU, engagement avg_session_duration / avg_messages_per_session, mood).
- **Verificar:** os eventos estão **realmente sendo capturados** (campos não voltam `null`), `INTERNAL_API_KEY` setada em prod, dashboard renderiza números reais após sessões de teste.
- **Adicionar SE não existir:** um sinal de **retorno não-incentivado** (sessões sem ter recebido lembrete/cutucada) — é o coração da hipótese. Se já houver proxy (DAU/WAU + session count), documentar como ler isso como "voltou sozinho".
- **Aceite:** após 2-3 sessões de teste, `/internal/analytics` devolve d1/engagement reais; founder consegue responder "tem retenção?" no D+7/D+14.
- **Limiar de leitura GO/NO-GO (definido — login NÃO conta, são 20 conhecidos):**
  - 🟢 **Verde:** ≥50% voltam no D+7 **sem cutucada** + sessões com profundidade (≥3-4 mensagens, não "oi/tchau").
  - 🟡 **Amarelo:** só voltam quando o founder lembra → produto não segura sozinho.
  - 🔴 **Vermelho:** <30% no D+7 mesmo conhecendo o founder → sinal forte de falta de fit.

---

## GATES DE PRODUÇÃO (Codex executa, founder fornece credenciais)

- [ ] Prod de pé + healthcheck PASS (`/health`).
- [ ] Migration `011_ai_features_enabled.sql` aplicada em prod (query confirmando coluna `profiles.ai_features_enabled`).
- [ ] **Signup travado na lista de convidados** (whitelist manual de e-mails OU invite code) — rando não entra.
- [ ] OpenAI ZDR confirmado + env prod `OPENAI_ZDR_CONFIRMED=true`.
- [ ] Sentry scrub: disparar erro sintético com CPF fake → confirmar **zero PII** no evento.
- [ ] Kill-switch: confirmar que dá pra **desligar a IA globalmente** (flag) e testar off/on. (war-room marcou como gap — validar se `ai_features_enabled` cobre global ou se falta toggle de sistema.)

---

## CHECK DE COPY (de graça, fácil esquecer — controle legal)
Rodar `grep -ri "terapia\|tratamento\|terapeuta\|cura\|diagnós" apps/web/src` e garantir que o **posicionamento ao usuário** não promete terapia/tratamento/cura. Termo: **apoio / companheiro de bem-estar**. (Dispara CFP-Psicologia + ANVISA se prometer tratamento.) Exceção: o disclaimer PODE usar "não substitui terapia" — isso é correto.

---

## Ordem sugerida
1. Item 1 (PF) assim que founder mandar os dados — destrava as páginas legais.
2. Gates de produção (prod live + migration + signup lock).
3. Itens 2-5 verificados contra prod.
4. Item 6 (retenção) + 2-3 sessões de teste pra confirmar captura.
5. Check de copy.
6. Reportar GO/NO-GO com evidência por item.

## O que o founder ainda precisa fornecer ao Codex
- Nome completo + CPF + endereço de contato (item 1).
- Confirmar mailbox `dpo@anipis.com.br` e `suporte@anipis.com.br` recebendo.
- Credenciais prod / confirmação ZDR / acesso pra rodar migration.

---

*Handoff gerado por Orion após conclave (patricia-peck / sean-duffy / eric-ries / ann-cavoukian). Código fica com o Codex; decisões não-código já fechadas acima.*
