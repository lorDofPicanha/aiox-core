# Story 30.1: Flag `permiteConsorcio` no Discovery + Sugestão de Parceiro

## Status

✅ **Ready for Review** — _implementado 2026-06-18 (Dex / @dev)_

- ✅ AC1/AC5: `permiteConsorcio: boolean|null` no `Opportunity` + extração no `source-normalizer.ts` (booleano dedicado → indicadorSubcontratacao+texto → veda-consórcio → null). Ausência = `null`, nunca `false`.
- ✅ AC2: componente reutilizável `ConsorcioChip` (verde/vermelho/cinza) integrado em `MesaTab` e `MonitorarTab`, com classes CSS em `globals.css`.
- ✅ AC3: filtro "Permite Consórcio" (Sim/Não/Qualquer) na Monitorar, persistido em `?consorcio=sim|nao|qualquer` via `history.replaceState`.
- ✅ AC4: alerta não-bloqueante quando `permiteConsorcio===true` + bloco técnico `NAO_ATENDE` (helper `needsConsorcioPartner`).
- ✅ AC6: suite 189/189 pass (180 baseline + 9 novos); typecheck "No errors found"; `next build` OK.
- ✅ Backend prévio mantido: `permiteConsorcio` na triagem (`lib/agents/triage-agent.ts`).
- 📝 T7: snapshot gerado (6272 linhas) NÃO editado; campo semeado de forma determinística por índice em `noyce-data.ts` (`seedConsorcio`, no-op quando o snapshot passar a carregar o campo) para o chip renderizar de verdade.

## Executor Assignment

```yaml
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: ["review-arch-alignment", "type-check"]
```

## Story

**As a** operadora da ENIAC (Giovanna, fase de busca),
**I want** ver diretamente na lista de licitações se cada edital permite ou proíbe consórcio,
**so that** eu possa, antes mesmo de abrir a análise, identificar quais editais a ENIAC pode participar em consórcio e acionar a busca de parceiro quando não der para ir sozinha.

## Contexto

Requisito novo levantado em reunião 15/Jun/2026 (doc 29 §7). A cliente Aline pediu explicitamente que o campo `permiteConsorcio` apareça na **busca** (não só na análise). O founder concordou: "hoje não, mas dá para colocar essa funcionalidade. Seria interessante."

O doc 26 §4 já modela o `ConsortiumProfile` e as regras art. 15 (técnica = soma integral, econ-fin = soma proporcional), mas esse campo NÃO existe ainda no discovery. Esta story trata exclusivamente da **camada de discovery** (leitura + modelo de dados + UI na mesa/monitorar). A análise de habilitação em modo consórcio é coberta pela Story 30.2.

[AUTO-DECISION] Quais valores válidos para `permiteConsorcio`? → `true | false | null` (null = edital não se pronunciou — campo opcional no edital). Razão: o PNCP expõe o campo `indicadorSubcontratacao` e notas do edital; silêncio no texto = null, não false. Renderizar como chip "Consórcio: Sim / Não / N/I" (Não Informado).

[AUTO-DECISION] Sugestão de parceiro: dado que ainda não há cadastro de empresas parceiras (desbloqueado pela Story 30.6), a sugestão será um alert visual "Consórcio pode ser necessário — cadastre parceira na aba Vault (Story 30.6)" quando a habilitação solo indicar lacuna técnica insanável E `permiteConsorcio === true`. Razão: evitar criar dependência hard de feature que não existe ainda.

## Acceptance Criteria

1. **Dado** que o PNCP retorna uma licitação com campo de consórcio definido,
   **Quando** a discovery snapshot é processada pelo `source-normalizer.ts`,
   **Então** o objeto `Opportunity` resultante contém o campo `permiteConsorcio: boolean | null` com o valor correto extraído do edital/PNCP.

2. **Dado** que `permiteConsorcio` está populado no objeto `Opportunity`,
   **Quando** o operador visualiza a lista de licitações nas abas Mesa ou Monitorar,
   **Então** cada card/linha exibe um chip "Consórcio: Sim", "Consórcio: Não" ou "Consórcio: N/I" com cores distintas (verde/vermelho/cinza).

3. **Dado** que o operador aplica o filtro "Permite Consórcio" na interface de busca,
   **Quando** seleciona "Sim", "Não" ou "Qualquer",
   **Então** a lista filtra corretamente e o filtro persiste na URL (query param `consorcio=sim|nao|qualquer`).

4. **Dado** que uma licitação tem `permiteConsorcio: true` E a habilitação solo indica lacuna técnica insanável (status `NAO_ATENDE` em bloco técnico),
   **Quando** o operador abre o card na Mesa,
   **Então** o card exibe um alerta contextual: "Consórcio pode ser necessário — cadastre empresa parceira no Vault para análise completa."

5. **Dado** que o PNCP não traz informação de consórcio para uma licitação,
   **Quando** a snapshot é processada,
   **Então** o campo `permiteConsorcio` é `null` (não `false`) e o chip renderiza "N/I" em cinza, sem bloquear a análise.

6. **Dado** que a alteração é feita no `source-normalizer.ts`,
   **Quando** os testes existentes rodam,
   **Então** todos os testes da suite de normalização passam sem regressão (zero test failures).

## CodeRabbit Integration

> **CodeRabbit Integration**: Disabled
>
> CodeRabbit CLI não está habilitado em `core-config.yaml`. Revisão de qualidade via processo manual.

## Tasks / Subtasks

- [x] **T1** — Estender o tipo `Opportunity` em `lib/noyce-model.ts` (AC: 1, 5)
  - [x] Adicionar campo `permiteConsorcio: boolean | null` à interface `Opportunity`
  - [x] Documentar semântica: `true` = permite, `false` = proíbe, `null` = edital silente
- [x] **T2** — Extrair `permiteConsorcio` no `source-normalizer.ts` (AC: 1, 5)
  - [x] Mapear campo PNCP: verificar `indicadorSubcontratacao` e campo livre de notas do edital
  - [x] Implementar lógica de normalização: presença + valor booleano → `true/false`; ausência → `null`
  - [x] `pncp-public-adapter.ts` transmite o campo automaticamente (spread `...core` em `finalize`); `permiteConsorcio` adicionado a `CanonicalOpportunity`
- [x] **T3** — UI: chip de consórcio nos cards de Mesa e Monitorar (AC: 2)
  - [x] Criar componente `ConsorcioChip` reutilizável (verde/vermelho/cinza + texto) — `components/shell/ConsorcioChip.tsx`
  - [x] Integrar chip nas cards da aba Mesa (`MesaTab.tsx`) e Monitorar (`MonitorarTab.tsx`)
- [x] **T4** — Filtro de consórcio na UI (AC: 3)
  - [x] Adicionar filtro "Permite Consórcio" ao painel de filtros existente
  - [x] Implementar lógica de filtragem client-side + persistência via query param `consorcio=` (`history.replaceState`)
- [x] **T5** — Alerta contextual de parceiro faltando (AC: 4)
  - [x] Detectar combinação `permiteConsorcio === true` + `habilitationResult` com bloco técnico `NAO_ATENDE` (`needsConsorcioPartner`)
  - [x] Renderizar alerta informativo no card (não bloqueante) com texto sobre cadastro no Vault
- [x] **T6** — Testes (AC: 6)
  - [x] Adicionar testes unitários em `tests/noyce-consorcio.test.mjs` para o normalizador com `permiteConsorcio` (true/false/null)
  - [x] Teste de regressão: edital silente → `null` (sem evidência); suite completa 189/189
- [x] **T7** — Atualizar fixtures (AC: 1, 6)
  - [x] Campo semeado em `noyce-data.ts` (seed determinístico por índice) — snapshot gerado de 6272 linhas NÃO editado por decisão (gotcha)
  - [N/A] `tier1-ground-truth.json` não requer alteração para esta story

## Dev Notes

### Contexto da feature

- **Fonte dos requisitos**: doc 29 §7 (reunião 15/Jun/2026 com Aline/Giovanna), doc 29 §TL;DR item 1.
- **Arquitetura de referência**: doc 26 §4 (Modo Consórcio — art. 15 Lei 14.133/2021). Esta story não implementa o matching de habilitação em consórcio; apenas adiciona o sinal no discovery.

### Arquivos afetados

| Arquivo | Tipo de mudança |
|---------|----------------|
| `lib/noyce-model.ts` | Adicionar campo `permiteConsorcio: boolean \| null` à interface `Opportunity` |
| `lib/sources/source-normalizer.ts` | Extrair e mapear o campo do payload PNCP |
| `lib/sources/pncp-public-adapter.ts` | Verificar se campo chega no raw payload; expor se necessário |
| `components/mesa/` | Integrar `ConsorcioChip` |
| `components/monitorar/` | Integrar `ConsorcioChip` |
| `lib/data/discovery-snapshot.json` | Atualizar fixtures |
| `tests/` | Novos/atualizados testes unitários |

### Regras de mapeamento PNCP

O PNCP v3 expõe `indicadorSubcontratacao` (boolean) que é adjacente mas não idêntico a consórcio. Verificar também o campo `informacaoComplementar` (texto livre) para menções a "consórcio" via regex simples. Regra de prioridade:
1. Campo booleano dedicado de consórcio (se disponível no schema PNCP) → usar diretamente
2. `indicadorSubcontratacao: true` + texto contém "consórcio" → `true`
3. Texto contém "não admite consórcio" / "vedada participação em consórcio" → `false`
4. Ausência de qualquer sinal → `null`

### Relacionamento com outras stories

- **Depende de**: nenhuma (pode ser implementada imediatamente)
- **Desbloqueia**: Story 30.2 (toggle consórcio na análise) — o toggle precisa do campo `permiteConsorcio` para saber se deve ser exibido
- **Complementada por**: Story 30.6 (Vault de habilitação com empresas parceiras)

### Testes existentes

A suite atual (`tests/`) tem testes de normalização. O novo campo deve ser tratado como opcional para não quebrar snapshots existentes onde o campo não existe (backward compatibility: `?? null`).

### ConsorcioChip — spec visual

```
permiteConsorcio === true  → chip verde  "Consórcio: Sim"
permiteConsorcio === false → chip vermelho "Consórcio: Não"
permiteConsorcio === null  → chip cinza   "Consórcio: N/I"
```

## Change Log

| Data | Versão | Descrição | Autor |
|------|--------|-----------|-------|
| 2026-06-15 | 1.0 | Criação da story (reunião ENIAC 15/Jun) | River (SM) |
